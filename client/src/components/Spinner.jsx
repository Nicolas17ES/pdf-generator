import { useState, useContext } from "react";

const Spinner = () => {

    return (

        <div className="spinner-container">
            Generating...
        </div>
        
    );
};

export default Spinner;